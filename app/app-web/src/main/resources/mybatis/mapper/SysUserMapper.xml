<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.app.dao.SysUserDAO">
    <resultMap id="BaseResultMap" type="SysUser">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="real_name" property="realName" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="department_id" property="departmentId" jdbcType="BIGINT"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="modify_date" property="modifyDate" jdbcType="TIMESTAMP"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 数据库列列表 -->
    <sql id="Base_Column_List">
		id, username, password, real_name, sex, phone, department_id, status, create_date, modify_date
	</sql>

    <!-- 插入SysUser到数据库 -->
    <insert id="insert" parameterType="SysUser" keyProperty="id">
		insert 
		into sys_user(username, password, real_name, sex, phone, department_id, status, create_date)
		values(#{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, #{realName,jdbcType=VARCHAR}, #{sex,jdbcType=VARCHAR}, 
		#{phone,jdbcType=VARCHAR}, #{departmentId,jdbcType=BIGINT}, #{status,jdbcType=VARCHAR}, #{createDate,jdbcType=TIMESTAMP})
	</insert>

    <!-- 通过SysUser的id更新SysUser中的数据 -->
    <update id="update" parameterType="SysUser">
        update sys_user
        <set>
        	modify_date=#{modifyDate}
            <if test="username != null and username != ''">,username=#{username}</if>
            <if test="password != null and password != ''">,password=#{password}</if>
            <if test="realName != null and realName != ''">,real_name=#{realName}</if>
            <if test="sex != null and sex != ''">,sex=#{sex}</if>
            <if test="phone != null and phone != ''">,phone=#{phone}</if>
            <if test="departmentId != null and departmentId != ''">,department_id=#{departmentId}</if>
            <if test="status != null and status != ''">,status=#{status}</if>
        </set>
        where id=#{id,jdbcType=BIGINT}
    </update>

    <!-- 通过username和id查询用户名是否存在 -->
    <select id="selectUsername" parameterType="SysUser" resultType="SysUser">
        select 
        <include refid="Base_Column_List"/>
        from sys_user
        where username=#{username,jdbcType=VARCHAR} and status='1' 
        <if test="id!=null">
        	and id!=#{id}
        </if>
    </select>

    <!-- 通过SysUser的id获得SysUser对象 -->
    <select id="selectByPrimaryKey" parameterType="long" resultType="SysUser">
        select
        <include refid="Base_Column_List"/>
        from sys_user 
        where id=#{id,jdbcType=BIGINT}
    </select>

    <!-- 获得SysUser数据集合 -->
    <select id="selectList" parameterType="SysUserDTO" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_user
        where 1=1
       	<if test="username != null and username != ''">
       		and username like concat('%', #{username},'%')
       	</if>
       	<if test="realName != null and realName != ''">
       		and real_name like concat('%', #{realName},'%')
       	</if>
       	<if test="sex != null and sex != ''">
       		and sex = #{sex}
       	</if>
       	<if test="phone != null and phone != ''">
       		and phone = #{phone}
       	</if>
       	<if test="departmentId != null">
       		and department_id = #{departmentId}
       	</if>
       	<if test="status != null and status != ''">
       		and status = #{status}
       	</if>
    </select>
</mapper>