<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.app.dao.SysMenuDAO">
    <resultMap id="BaseResultMap" type="SysMenu">
		<id column="id" property="id" jdbcType="BIGINT"/>
		<result column="name" property="name" jdbcType="VARCHAR"/>
		<result column="parent_id" property="parentId" jdbcType="BIGINT"/>
		<result column="url" property="url" jdbcType="VARCHAR"/>
		<result column="icon" property="icon" jdbcType="VARCHAR"/>
		<result column="rank" property="rank" jdbcType="INTEGER"/>
    </resultMap>
    <!-- 数据库列列表 -->
    <sql id="Base_Column_List">
		id, name, parent_id, url, icon, rank
	</sql>

    <!-- 获得数据集合 -->
    <select id="selectListByParentIdAndUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_menu sm
        where 1=1
		<if test="parentId != null">
			and parent_id=#{parentId}
		</if>
        <if test="userId != null">
        	and exists (
        		select * from sys_relation sr1,sys_relation sr2
        			where sr1.relation_type='A' 
        				and sr1.main_primary_id=#{userId}
        				and sr2.relation_type='B' 
        				and sr1.rel_primary_id=sr2.main_primary_id
        				and sr2.rel_primary_id=sm.id
        			)
        </if>
        order by rank
    </select>
</mapper>