<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.app.dao.remote1.SysUserDAO1">
    <resultMap id="BaseResultMap" type="SysUser">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 数据库列列表 -->
    <sql id="Base_Column_List">
		id, name, password
	</sql>

    <!-- 通过SysUser的id获得SysUser对象 -->
    <select id="selectByPrimaryKey" parameterType="long" resultType="SysUser">
        select
        <include refid="Base_Column_List"/>
        from sys_user 
        where id=#{id,jdbcType=BIGINT}
    </select>

    <!-- 获得SysUser数据集合 -->
    <select id="selectList" parameterType="java.util.HashMap" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_user
        where 1=1
       	<if test="name != null and name != ''">
       		and name like concat('%', #{username},'%')
       	</if>
       	order by name asc
    </select>
</mapper>